const largeNumber = 5;
const smallNumber = 3;
const targetNumber = 4;

let stepCounter = 0;

const smallContainer = [];
const largeContainer = [];

// ============================================

function valueChecker (lgNum, smNum, trgtNum) {}

function start () {
    if (smHolder != targetNumber && lgHolder != targetNumber) {
        fillLarge();
    } else {
        console.log("smHolder: " + smHolder + " || lgHolder: " + lgHolder);
        valueChecker();
    }

    stepCounter++;
};